dataset: mnist
image_size: 28
in_channels: 1
num_classes: 10
null_label: 10

timesteps: 1000
batch_size: 128
epochs: 20
lr: 0.0002

base_channels: 64
time_dim: 256
drop_label_prob: 0.1

save_dir: checkpoints/
checkpoint: checkpoints/mnist_cfg_unet.pt
figures_dir: figures/day4/
